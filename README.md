# Birthday Gift Website 🎉

A premium-looking, responsive birthday web page made with plain HTML, CSS, and JavaScript.

## Project Structure

- `index.html` — page structure
- `styles.css` — styling and responsive design
- `script.js` — interactions, animations, music, clocks, and game logic
- `arpita1.jpg` to `arpita5.jpg` — background images
- `romantic.mp3` — background music

## Features

- Multi-section birthday experience with smooth navigation
- Real-time age and next birthday countdown
- Confetti celebration (manual + auto burst)
- Single-track music player (play/pause + volume)
- Breakout mini-game (touch + keyboard support)
- Mobile, tablet, and desktop responsive layout

## Run Locally

1. Keep all files in the same folder.
2. Open `index.html` in your browser.

## Upload to GitHub

### Option 1: GitHub Website (No Terminal)

1. Create a new repository on GitHub (for example: `birthday-gift`).
2. Click **Add file** → **Upload files**.
3. Upload all files from this folder.
4. Commit changes.
5. Open `index.html` from the repo, or enable GitHub Pages.

### Option 2: Using Git Commands

```bash
git init
git add .
git commit -m "Initial birthday gift website"
git branch -M main
git remote add origin https://github.com/<your-username>/<repo-name>.git
git push -u origin main
```

## (Optional) Enable GitHub Pages

1. Go to **Repository Settings** → **Pages**.
2. Under **Build and deployment**, choose **Deploy from a branch**.
3. Select branch `main` and folder `/ (root)`.
4. Save and wait for the site URL to be generated.

## Notes

- If images/music do not load, verify file names match exactly (including extension and case).
- For best mobile behavior, test once after a hard refresh.
